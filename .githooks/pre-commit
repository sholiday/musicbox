#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
pushd "$repo_root" >/dev/null
echo "Verifying cargo build --all-targets --all-features..."
cargo build --all-targets --all-features
echo "Running cargo test before commit..."
cargo test
popd >/dev/null
